export default function MinimalTestPage() {
  // This is a completely static page with no client components
  return (
    <div style={{ padding: '2rem' }}>
      <h1 style={{ marginBottom: '1rem' }}>Minimal Test Page</h1>
      <p>This is a simple static page with no JavaScript or client components.</p>
    </div>
  );
}
