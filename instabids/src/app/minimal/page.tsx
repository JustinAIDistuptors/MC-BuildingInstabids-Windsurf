'use client';

import React from 'react';

export default function MinimalPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1 style={{ fontSize: '2rem', fontWeight: 'bold', marginBottom: '1rem' }}>
        Minimal Test Page
      </h1>
      <p>This is a simple test page to verify basic rendering.</p>
    </div>
  );
}
